// const sorter = field => (a, b) => {
//     if (a[field] < b[field])
//         return -1;
//     if (a[field] > b[field])
//         return +1;
//     else
//         return 0;
// };

// const normaliser = entry => {
//     const result = entry;
//     result.name = result.name.substring(result.name.indexOf('-')).toUpperCase();
//     return result;
// };
